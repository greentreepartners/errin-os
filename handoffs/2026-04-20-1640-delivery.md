# errin-os Handoff — 2026-04-20 16:40 — delivery

**Project:** errin-os
**Role:** delivery
**Date:** 2026-04-20
**From:** Previous delivery chat (Brief 2A-1 + 2A-Closeout sequence)
**Status:** Brief 2A-1 layout shell shipped to https://errin-os.netlify.app/. D8 reconciled to match execution-co reality. Five conventions lessons codified. Repo at `7138157`. Ready for Brief 2A-2 (visual polish + footer-SHA fix).

---

## What this chat decided / shipped / changed

Three commits landed on `main` plus one config commit:
- `655d900` — Brief 2A-1 Talent Agent layout shell shipped to https://errin-os.netlify.app/
- `1f0ba1d` — D8 in DECISIONS.md rewritten to match execution-co reality (white on near-black, cyan accent, three-font stack, section-numbered headers); reconciliation managed via Strategy escalation
- `d2a9339` — Five conventions lessons codified in CLAUDE.md (§1.1) and docs/PM_PLAYBOOK.md
- `7138157` — handoff.config.md committed (handoff infrastructure, not chat-driven content)

Decisions made and now in canonical docs:
- D8 fully rewritten (DECISIONS.md)
- Cyan semantic rule pinned to two uses: active filter pill + `in_progress` task badge (DECISIONS.md D8)
- Three-font stack (serif/sans/mono) ports to dashboard in full (DECISIONS.md D8)
- Section-numbered headers port to project level only, not motion subsections (DECISIONS.md D8)
- Single-quoted printf for commit messages forbidden [HARD] (CLAUDE.md §1.1)
- Speculative/forward-looking commit body content forbidden (CLAUDE.md §1.1)
- Truncated chained-command permission dialogs must be denied (PM_PLAYBOOK.md)
- Deferred-verification flag required for shipped-but-unrendered code paths (PM_PLAYBOOK.md)
- Netlify push-to-live timing corrected from "20 min" to "<1 min" (PM_PLAYBOOK.md)

Decisions not yet in canonical docs:
- Footer-SHA `local-dev` bug deferred to Brief 2A-2 (folded into 2A-2 scope, no doc update yet)
- Three candidate conventions updates observed but not codified: stronger cat-display rule, pre-flight emphasis, visual-reference verification (PM proposes these in next Strategy escalation)

## What's open

**Brief 2A-2 — visual polish (the immediate next brief).**
Apply the new D8 dialect to the Brief 2A-1 layout shell. Specifically:
- Implement near-black background (~#0a0a0a) + white/off-white body + electric cyan accent (~#7ddfee), all matched exactly to execution-co.netlify.app
- Load actual font files via Next.js font system for the three-font stack (serif/sans/mono); match execution-co's specific font choices
- Re-style filter pills with monospace + all-caps + cyan border on active state
- Re-style cards with mono short_ids, sans titles + descriptions, mono badges
- Re-label project sections as `01 · CAREER`, `02 · NAMECOACH` (Life is private, won't render)
- Apply cyan treatment to NC1's `in_progress` status badge (the only public task currently in that state)
- Preserve all 2A-1 structural decisions: PUBLIC badge anti-overlap (`pt-8` + `absolute top-2 right-2`), visibility border treatments (solid/dashed), dependency pill suppression on private blockers

**Folded into 2A-2 scope: footer-SHA `local-dev` bug.**
Production footer reads `Build commit: local-dev` instead of the actual short SHA. Cause: `process.env.COMMIT_REF` not being injected by Netlify into the build environment. Diagnose during 2A-2; potential fix is `process.env.COMMIT_REF || process.env.NETLIFY_COMMIT_REF` belt-and-braces, or restoring the env var if dropped from Netlify config.

**Three candidate conventions updates pending PM proposal to Strategy:**
1. Stronger cat-display rule: agent must paste cat output inline, not merely confirm cat ran
2. Pre-flight emphasis: all checks must complete and report in conversation before any file modification
3. Visual-reference verification: PM should fetch and verify visual references match decision wording before drafting briefs that depend on them

Successor PM chat should escalate these to Strategy at appropriate moment (probably end of 2A-2 closeout, batched with whatever else accumulates).

**Brief 2A-1 dashed-border CSS class shipped unverified.**
Public view exercises only public cards (which get solid borders). Dashed branch is correct in code but unverified in render. Brief 2B (admin route, first to render private cards) must include explicit verification that dashed renders correctly. Logged per the new deferred-verification rule in PM_PLAYBOOK.md.

## What the successor must know

**Surface quirks observed in this chat (carry forward):**
- Diff badge in Claude Code permission UI can lag actual file state mid-edit. Don't act on diff-size discrepancies until step completes.
- Agent has twice claimed cat output ran without showing it inline — required PM pushback. New cat-display rule is in the candidate conventions queue.
- Agent edited `DECISIONS.md` once before completing pre-flight checks. Pre-flight emphasis rule is in the candidate queue.
- Permission dialog still flattens long commands visually; verify via agent's prose, not the dialog. (Already in PM_PLAYBOOK.md per Phase 1.)
- Completion `›` chevrons still hide tool output; require expansion. (Already in CLAUDE.md §1.5 per Phase 1.)

**Operator preference observed mid-chat:**
Operator prefers tappable input options for clarification questions over free-text prompts when on mobile. Use `ask_user_input_v0` proactively. Operator also explicitly asked PM to give recommendations *with reasoning* when posing options, not just labelled defaults — "you usually give me a recommendation and tell me why, please do this." Apply consistently.

**Operator pace expectation:**
Light push toward speed when PM gets too explanatory ("wow thats too much content, stop with the explanation and just give me clear commands, and where to run them"). When that signal lands, strip prose, give pasteable command blocks. Doesn't mean skip review steps — means deliver them in shorter form.

**Altitude drift to watch (from diagnostic):**
This chat drafted full conventions wording before Strategy review. Strategy approved with one wording amendment, so the boundary held in practice. Successor PM should either (a) follow the precedent (PM proposes wording, Strategy approves/edits) or (b) correct it (PM lists raw lessons, Strategy drafts wording from scratch). Operator chose to log the precedent rather than escalate now — successor decides on the next conventions update.

**Repo state observation:**
`main` is at `7138157` (handoff config commit, post-d2a9339). The handoff doc this chat is about to commit will become the next commit on `main`. Successor pulls from there.

**Strategy project status:**
The Strategy project (separate Claude project, GSTACK CEO brain) was active during this chat for D8 reconciliation and conventions wording approval. Strategy's Claude Code session committed `1f0ba1d` (D8 swap) directly. PM project committed `d2a9339` (conventions). Both reachable via the same repo. Successor PM does not need to coordinate with Strategy unless escalation needed per existing criteria.

## Setup gaps inherited

One altitude drift logged (per operator decision): PM drafted full conventions wording before Strategy review. Recommended action for successor: pick a posture on the next conventions update and apply consistently. Either follow the precedent set here, or correct it — both defensible, neither a rule until the operator confirms.

## What's NOT in this handoff

The successor should read these from the repo before doing other work:
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/DECISIONS.md
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/VISIBILITY.md

## First instruction to the successor

Paste the bootstrap prompt (see Step 7 output) into a new chat in the errin-os delivery project. The successor will fetch this handoff and the canonical docs before responding.
