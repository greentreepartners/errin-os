# errin-os Handoff — 2026-04-21 11:45 — delivery

**Project:** errin-os
**Role:** delivery
**Date:** 2026-04-21
**From:** Previous delivery chat (session following real data migration handoff at 2026-04-21 09:30)
**Status:** Four briefs shipped clean. Public footer D6-compliant, public filter pills wired, legend removed, admin multi-select filter pills wired (status + project), CLAUDE.md extended with five new conventions (§1.11, §1.12, §1.13, §1.14, §1.15 — permission prompts, dictation exemption, RSC islands, shared D8 constants, grep pitfall). Public and admin surfaces are both functionally complete for v1 filtering. Next queued: 2B-Fix-8 (card density — horizontal badge row on admin) and the original Brief B (admin form expansion for short_id, effort, impact, horizon, is_gate).

---

## What this chat decided / shipped / changed

**Brief 2B-Fix-2 — Footer D6-compliance (`6db9c36`):**
Created Postgres RPC `public.public_counts()` with `security definer` returning `{public_count, total_count}`. Wired into public page via `supabase.rpc()` parallel with existing projects fetch. Footer now renders "Showing X of Y tasks. Z tasks are private." with monospace numbers per D8 typographic rule. Fixed hardcoded "4 of 4" bug. Single file `src/app/page.tsx` changed.

**Brief 2B-Fix-3 — CLAUDE.md §1.11 (`91689d7`):**
Added plan-scoped permission prompts convention. Codifies which Claude Code permission prompts can go "Always allow" vs "Allow once" vs "Deny and flag" after operator replies APPROVED on a plan. `[HARD]` rule: `git commit` and `git push` never go Always allow regardless of which button Claude Code offers. Table also covers edits, builds, dev servers, temp cleanup, scope drift.

**Brief 2B-Fix-4 — Public filter pills + legend removal (`9df8908`):**
Wired status filter pills (ALL, THIS WEEK, IN PROGRESS, BLOCKED, DONE) on public view. Single-select with ALL as reset-without-highlight. Active pill uses D8 cyan. Removed redundant `Horizon / Effort / Impact` legend block — card badges are self-labelling. First application of RSC client-island pattern: extracted `src/app/_components/TaskFilter.tsx` as `"use client"` component receiving server-fetched `projects` as prop; page stayed Server Component for the footer RPC fetch.

**Brief 2B-Fix-5 — Admin multi-select filter pills (`d518c1c`):**
Two filter rows on admin: status (6 pills: BACKLOG, THIS_WEEK, IN_PROGRESS, BLOCKED, DONE, KILLED) and project (CAREER, NAMECOACH, LIFE in display_order). Multi-select semantics with zero-state-as-ALL (no explicit ALL pill). Filter state is local React `useState<Set<...>>`, no URL params, no persistence. Intentional D7 divergence from public (single-select + ALL pill) committed and code-commented at top of `AdminTaskFilter.tsx`. Extracted shared class-string constants to `src/lib/filter-pill.ts` — imported by both public and admin filter surfaces. Four files changed: new `filter-pill.ts`, new `AdminTaskFilter.tsx`, modified `TaskFilter.tsx` (swap local consts for import — runtime byte-identical on public), modified `src/app/admin/page.tsx` (swap card render loop for `<AdminTaskFilter />`).

**Brief 2B-Fix-6 — CLAUDE.md §1.12 (`be8a5b1`):**
Added verbatim-dictation exemption from Plan Mode. Three conditions required: exact content dictated, exact commit message dictated, single file. `[HARD]` — §1.7 four-gate sequence and §1.1 printf pattern still apply; only Plan Mode is skipped. This commit also retroactively added the §1.11 cross-reference into the "Plan-Mode-then-APPROVED pattern" section (which 2B-Fix-3 should have included but missed — caught during anchor verification). Convention was first tested on itself with the anchor-clarification pause; worked as designed.

**Brief 2B-Fix-7 — CLAUDE.md §1.13 / §1.14 / §1.15 (`1fe0a61`):**
Three technical conventions captured:
- §1.13 RSC client-island pattern for interactive state over server-fetched data
- §1.14 Shared class-string constants as D8 semantic-rule guardrail
- §1.15 [HARD] `grep -oE | wc -l` for occurrence counts, never `grep -c` on HTML

First production use of §1.12 — conditions cleanly met, Plan Mode skipped, execution discipline preserved. Exemption saved a plan artifact that would have just restated the brief. Working as designed.

**Operator auto-approve list established (not yet committed to CLAUDE.md):**
During execution, operator began self-approving permission prompts for read-only and scoped-temp-cleanup actions without consulting the delivery chat. Rule set working-memory: read-only git commands, `wc -l`, `cat`, `ls`, `grep`, `head`, `tail`, `curl localhost`, and `rm -f /tmp/<named-file>` (single file, created this session, no wildcards, not recursive). Candidate for future CLAUDE.md §1.16. **Flagged for Strategy consideration** — this convention changes the human-in-the-loop pattern materially; operator may want Strategy to bless it before codifying.

## What's open

**Brief 2B-Fix-8 — Card density / horizontal badge row (admin, possibly public):**
Operator flagged mid-session: current card badge layout is a right-aligned vertical stack, making cards 3-4 lines tall. With 49 tasks on admin, critical work scrolls off-screen. Proposed fix: horizontal badge row across the bottom of each card, reduce top/bottom padding. Five clarifying questions held over to next session:
1. Admin only, or both surfaces? (Lean: admin only — public is portfolio, airier cards defensible)
2. What badges go in the bottom row? (Horizon, effort, impact, visibility — four inline? Or three with visibility elsewhere?)
3. Status pill stays top-row inline with title? (Lean: yes, primary state signal)
4. Padding reduction from current `p-4` to something tighter?
5. Dependency pill location in new layout?

**Brief B — Admin form expansion:**
Already queued. Admin add form is 4-field (Project, Title, Description, Motion — Career only). Needs expansion to cover short_id, effort, impact, horizon, is_gate. Estimated 60-90 min with UX decisions (dropdown order, required vs optional, default values, full-form vs quick-add UX shape). Should probably follow 2B-Fix-8 since that's the smaller win and form expansion benefits from a fresh session anyway.

**Untracked file cleaned:**
`learnings.md` untracked artifact from aborted 2B-Fix-7 first dispatch was deleted after §1.13-§1.15 landed in CLAUDE.md. Working tree clean post-2B-Fix-7.

**Escalation queue for next Strategy session (grows from previous handoff's ~13 to ~16 candidate items):**
- (new this session) Operator auto-approve list candidacy for §1.16
- (new this session) §1.12 might benefit from tightening "touches a single file" to "touches the locations the operator specified" — but operator and delivery agreed *against* this after meta-analysis of first §1.12 test; leaving as-is for now, flag if repeatedly hits anchor issues
- (new this session) GSTACK PM instructions reference `/projects/<slug>/public/learnings.md` folder structure that was never built. In practice CLAUDE.md has absorbed the learnings-capture role. Instructions should be updated to reflect reality, or the folder structure built out (and existing docs migrated). Low priority.
- All existing items from prior session still queued (SCHEMA.sql drift, admin add latency 6-7s, admin add UX gap for short_id etc., Phase 3a Claude skill still outstanding, etc.)

## What the successor must know

- **§1.11 / §1.12 / §1.13 / §1.14 / §1.15 are all active conventions.** A new delivery chat should internalise these from CLAUDE.md on session start. §1.11 especially changes permission-prompt behaviour — proactively tell operator what's safe to Always-allow after APPROVED.
- **Operator became materially faster at git-gate self-approval over the course of this session.** Bootstrap chat approved every gate with delivery; by mid-session operator was self-driving the four-gate sequence and paste-reporting results. This is expected to continue. Do not regress to approval-chaperoning for read-only gates.
- **RSC client-island pattern is established for both public and admin.** Third application (likely in 2B-Fix-8 or Brief B) will be materially faster — the architectural call is settled. See §1.13 for pattern, reference `TaskFilter.tsx` and `AdminTaskFilter.tsx` as worked examples.
- **D7 divergence between public (single-select + ALL pill) and admin (multi-select + zero-state-as-ALL) is intentional.** Both idioms coexist deliberately. Do not harmonise without Strategy escalation. Comment at top of `AdminTaskFilter.tsx` makes this visible at the code site.
- **Operator prefers working in Claude Code desktop app (Plan Mode) per D11.** Do not suggest Cursor or CLI-primary workflows.
- **Session length this chat: ~2h15m across 6 briefs.** Operator explicitly called fatigue as the reason to stop before Brief B (admin form expansion). Respect that pattern — when operator flags a clean break, take it.

## What's NOT in this handoff

The successor should read these from the repo before doing other work:
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/handoff.config.md
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/DECISIONS.md
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/VISIBILITY.md
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/CLAUDE.md
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/AGENTS.md
- https://raw.githubusercontent.com/greentreepartners/errin-os/main/FUTURE.md

## First instruction to the successor

Most likely next brief: **Brief 2B-Fix-8 (card density — horizontal badge row on admin).** Small-to-medium UX brief, benefits from fresh head. Five clarifying questions above need operator answers before Plan Mode. After 2B-Fix-8, Brief B (admin form expansion) is the next substantial work.
